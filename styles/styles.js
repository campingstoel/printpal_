//import all styles and export them as a single object
import button from './button';
import header from './header';
import navbar from './navbar';
import searchBar from './searchbar';

export default {
    button,
    header,
    navbar,
    searchBar
};
