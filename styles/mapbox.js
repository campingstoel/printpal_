import { StyleSheet } from 'react-native';

const mapbox =  StyleSheet.create({

    container: {
        width: '100%',
        height: 250,
        overflow: 'hidden',
        marginBottom: 20,

    },
    
});

export default mapbox