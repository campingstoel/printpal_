import { StyleSheet } from 'react-native';

const mapbox =  StyleSheet.create({

    container: {
        width: '100%',
        height: '100%',
        borderRadius: 20,
    },
    
});

export default mapbox