import { StyleSheet } from 'react-native';

const mapbox =  StyleSheet.create({

    container: {
        width: '100%',
        height: 250,
        borderRadius: 30,
        overflow: 'hidden',
    },
    
});

export default mapbox